return {
	chatByZone = true, --Chat only work by zones (This function only work if chat version is "LegacyChatService". Change this in "TextChatService" -> "ChatVersion" -> "LegacyChatService")
	topBarInfo = true --Show current zone in the top bar (recomended)
}